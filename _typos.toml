[default]
locale = "en"
extend-ignore-re = [
    # Test examples with intentional typos for suggestion testing
    "adnlp_backen",
    "ipopt_backen",
    # Common variable names in our codebase
    "strat",
]

[files]
extend-exclude = [
    "*.json",
    "*.toml",
    "*.svg",
]
