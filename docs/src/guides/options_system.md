# Options System

```@meta
CurrentModule = CTSolvers
```

!!! warning "Work in Progress"
    This page is under construction. See kanban task 08 for the planned content.

## OptionDefinition

TODO: Creating option definitions with name, type, default, description, aliases, validator.

## OptionValue and Provenance

TODO: The 3 sources (`:user`, `:default`, `:computed`) and provenance tracking.

## StrategyMetadata

TODO: Assembling OptionDefinitions into StrategyMetadata, Collection interface.

## StrategyOptions

TODO: Construction via `build_strategy_options`, value access, introspection.

## Validation Modes

TODO: Strict mode (Levenshtein suggestions), permissive mode (warnings).

## Extraction Functions

TODO: `extract_options` and `extract_raw_options`.
